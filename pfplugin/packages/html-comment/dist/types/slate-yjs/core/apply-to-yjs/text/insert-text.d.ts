import type Y from 'yjs';
import { SlateNode } from '@wangeditor/editor';
import { InsertTextOperation } from '../../types';
export declare function insertText(sharedRoot: Y.XmlText, slateRoot: SlateNode, op: InsertTextOperation): void;
