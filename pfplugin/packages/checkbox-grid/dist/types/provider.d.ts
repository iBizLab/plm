import { IControlProvider } from '@ibiz-template/runtime';
/**
 * 多选视图表格插件（多选框）适配器
 *
 * @export
 * @class CheckboxGridProvider
 * @extends {IControlProvider}
 */
export declare class CheckboxGridProvider implements IControlProvider {
    component: string;
}
