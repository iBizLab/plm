import { IControlProvider } from '@ibiz-template/runtime';
/**
 * 多选视图右侧列表适配器
 *
 * @export
 * @class SampleListProvider
 * @implements {IControlProvider}
 */
export declare class SampleListProvider implements IControlProvider {
    component: string;
}
