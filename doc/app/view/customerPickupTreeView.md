# 客户(customerPickupTreeView)  <!-- {docsify-ignore-all} -->






## 控件
  * 树视图(tree)
  * 搜索栏(searchbar)
  * CAPTIONBAR(captionbar)


<script>
 const { createApp } = Vue
  createApp({
    data() {
      return {
        message: '!'
      }
    }
  }).use(ElementPlus).mount('#app')
</script>