# 迭代报表(sprint_bi_report)  <!-- {docsify-ignore-all} -->


<script>
 const { createApp } = Vue
  createApp({
    data() {
      return {
      }
    },
    methods: {
    }
  }).use(ElementPlus).mount('#app')
</script>