# 排期选择(product_planPickupTreeView)  <!-- {docsify-ignore-all} -->






## 控件
  * 搜索栏(searchbar)
  * 树视图(tree)
  * CAPTIONBAR(captionbar)


<script>
 const { createApp } = Vue
  createApp({
    data() {
      return {
        message: '!'
      }
    }
  }).use(ElementPlus).mount('#app')
</script>